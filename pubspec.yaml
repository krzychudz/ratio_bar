name: ratio_bar
description: "A simple and customizable Flutter widget for displaying a ratio bar, which visually represents the proportion between two values."
version: 1.0.1
repository: https://github.com/krzychudz/ratio_bar
issue_tracker: https://github.com/krzychudz/ratio_bar/issues

environment:
  sdk: '>=2.19.4 <4.0.0'
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^4.0.0


flutter: