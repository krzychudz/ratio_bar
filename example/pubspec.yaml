name: example
description: "Ratio bar package example project"

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.5.0-172.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  ratio_bar:
    path: ../
  

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true